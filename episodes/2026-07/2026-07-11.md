# 2026-07-11

## memory-agent D1：TypeScript/npm 骨架 + 三路 hello + MCP hello

今天正式开始 memory-agent 10 天冲刺 D1。

### 代码仓库落地

- 在 `/Users/wenbin/repos/memory-agent` 初始化 TypeScript 项目骨架。
- 包管理器从原计划 pnpm 改为 npm：`package.json` + `package-lock.json`。
- 配好 `tsconfig.json`、Biome、Vitest、VS Code 项目设置。
- 依赖版本从 `latest` 收紧到当前验证过的 caret 版本，避免后续 install 漂移。
- Node 24 可以直接运行 `.ts`，所以 demo 和 MCP server 都用 `node *.ts`，不依赖 `tsx`。

### 三路 hello

- `demos/d1-langgraph-hello.ts`：LangGraph.js StateGraph 双节点 `researcher -> writer`。
- `demos/d1-ai-sdk-hello.ts`：Vercel AI SDK + typed tool，默认离线跳过真实模型。
- `demos/d1-while-loop-hello.ts`：手写 while-loop 工具循环，强化“agent = loop + tools + memory”的心智模型。

### LiteLLM / localhost:4000

- AI SDK demo 改成通过 `@ai-sdk/openai` 连接本地 LiteLLM proxy：`http://localhost:4000/v1`。
- 必须用 `litellm.chat(model)` 走 Chat Completions；默认 Responses API 会打到 `/v1/responses`，当前 LiteLLM/GitHub Copilot 后端会报 `invalid_request_body`。
- `MEMORY_AGENT_REAL_LLM=1 npm run demo:ai-sdk` 已实测返回 `Hello from memory-agent D1.`。
- 强制 `toolChoice: "required"` 在当前后端会卡住，因此 D1 先保持自动 tool choice。

### MCP hello

- `src/server.ts` 暴露 `ping` tool。
- repo 级 `.mcp.json` 指向 `node src/server.ts`。
- `tests/mcp-client-smoke.ts` 能 list/call `ping`，证明 MCP 协议链路可用。

### 验证

- `npm run typecheck` ✅
- `npm run check` ✅
- `npm test` ✅
- `npm run build` ✅
- `npm run demo:langgraph` ✅
- `npm run demo:while-loop` ✅
- `npm run demo:ai-sdk` ✅（默认离线跳过）
- `npm run smoke:mcp` ✅
- `MEMORY_AGENT_REAL_LLM=1 npm run demo:ai-sdk` ✅

### 提交

- `memory-agent`: `e76d06d Initialize D1 TypeScript MCP scaffold`
- `acFens`: `377b6d4 Update memory-agent D1 notes`（这次先直接 commit 了，随后通读 acFens 发现规范要求通过 branch + PR 更新，后续要按规范修正流程）

## 通读 acFens，重新确认规范

今天又通读了一遍 acFens 的 identity、goals、lessons、episodes、skills、workspace conventions。

关键规范重新确认：

- acFens 是主记忆系统，不是普通笔记目录。
- 记忆更新要走 branch + PR，不应直接 commit main。
- 重要事件要当天写 episode；稳定规则再从 lessons 提炼到 beliefs/principles。
- 输出和提交前要主动扫敏感信息。
- 重启、删除、停服务等中断性操作必须先得到老王明确许可。

## 明天 D2

- frontmatter schema 定稿，写成 Zod schema + TS type。
- SQLite 建表：`memories / relations / ingest_log`。
- 初始化 `~/memory/` 数据 repo 骨架。
- 起 `src/schema.ts` 和 `src/store/sqlite.ts`。
